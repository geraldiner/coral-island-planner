const TILE_SIZE = 16;

const DEFAULT_ITEM = {
  displayName: 'Crop',
  sprite: 'crop',
  isCrop: true,
  tileCoverage: null,
};

const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

export { DEFAULT_ITEM, SVG_NAMESPACE, TILE_SIZE };
